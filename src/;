mod backend;
use backend::*;

fn main() {
    let mut backend = SectionManager::load_from_file();

    println!("{:?}", backend);
    
    backend.select_section("Todoing");

    backend.save_to_file();
}
